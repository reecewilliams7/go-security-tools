## gst client-credentials create

Creates a Client ID and Secret that can be used as Client Credentials in OAuth2.0 and OpenID Connect

### Synopsis

Creates a Client ID and Secret pair for use with OAuth2.0 and OpenID Connect flows.

```
gst client-credentials create [flags]
```

### Options

```
  -t, --client-id-type string       The type of Client ID to create. Options are 'uuidv7', 'short-uuid', and 'nanoid'. (default "uuidv7")
  -s, --client-secret-type string   The type of Client Secret to create. Options are 'crypto-rand'. (default "crypto-rand")
  -c, --count int                   The count to create. (default 1)
  -h, --help                        help for create
  -e, --secret-encoding string      Encoding for the client secret. Options are 'base64', 'base64url', and 'hex'. (default "base64")
  -l, --secret-length int           Number of random bytes to use for the client secret (16–64). (default 32)
```

### SEE ALSO

* [gst client-credentials](gst_client-credentials.md)	 - Tools for working with Client Credentials

###### Auto generated by spf13/cobra on 21-Apr-2026
